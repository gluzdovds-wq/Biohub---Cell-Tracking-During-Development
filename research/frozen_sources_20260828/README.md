# Frozen public sources for EXP078–082

These are exact reviewed public-source snapshots, not models newly authored or
trained by us. Authors, source notebook URLs (owner/slug), version numbers,
Kaggle script-version IDs, SHA-256 values and account submission receipts are
recorded in `../../reports/submission_batch_20260828.json`.

Sources: Rishabh Roy (`rishabhr0y`, SDW70 v1), `flexonafft` (Flex v22),
`arnav170` (SDW75, VEL10 and MTL8, all v1). Attribution is retained in the
original notebooks. Author leaderboard scores are distinct from account scores.

Git newline conversion is disabled for these notebooks so checked-out bytes
continue to match the reviewed SHA-256. Output CSVs and microscopy data are not
included here. The installed Kaggle output CLI ignores version suffixes; do not
substitute a later output for these version-specific receipts.
